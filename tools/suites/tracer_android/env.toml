# Environment configuration for tracer_android host-side checks

[run_control]
enable_environment_clean = 1
enable_environment_prepare = 1

[paths]
build_app_name = "tracer_android"
default_build_dir = "build"
target_executables_dir = "${out_test_root}/artifact_android/workspace"
output_dir_name = "${out_test_root}/artifact_android/artifacts"
py_output_dir = "${out_test_root}/artifact_android/logs"
test_data_path = "${test_root}/data"

[cli_names]
executable_cli_name = "cmd.exe"
executable_app_name = "tracer_android_host_checks"

[cleanup]
files_to_copy = ["cmd.exe"]
folders_to_copy = []
