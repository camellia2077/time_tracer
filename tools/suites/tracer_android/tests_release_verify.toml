# Extra checks for android_release_verify profile

[[commands]]
name = "Release Report LintRelease Exists"
stage = "android-release-verify"
args = [
  "/c",
  "if exist ..\\..\\..\\..\\apps\\android\\app\\build\\reports\\lint-results-release.html (exit /b 0) else (echo Missing lint-results-release.html && exit /b 1)"
]
expect_exit = 0
