[run]
name = "tracer_core_pipeline"
continue_on_failure = false

[scope]
paths_file = "../pipeline.paths"

[defaults]
kind = "verify"
app = "tracer_core_shell"
profile = "cap_pipeline"
concise = true

[[tracks]]
name = "cap_pipeline"
