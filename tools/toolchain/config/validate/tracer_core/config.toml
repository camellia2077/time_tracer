[run]
name = "tracer_core_config"
continue_on_failure = false

[scope]
paths_file = "../config.paths"

[defaults]
kind = "verify"
app = "tracer_core_shell"
profile = "cap_config"
concise = true

[[tracks]]
name = "cap_config"
