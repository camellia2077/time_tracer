[run]
name = "tracer_core_persistence_runtime"
continue_on_failure = false

[scope]
paths_file = "../persistence_runtime.paths"

[defaults]
kind = "verify"
app = "tracer_core_shell"
profile = "cap_persistence_runtime"
concise = true

[[tracks]]
name = "cap_persistence_runtime"
