from __future__ import annotations

INSIGHTS_PERIOD_KEYS: tuple[str, ...] = ("day", "month", "period", "week", "year")
INSIGHTS_FORMAT_ORDER: tuple[str, ...] = ("markdown", "latex", "typst")
SUPPORTED_TARGETS: tuple[str, ...] = ("windows", "android")
