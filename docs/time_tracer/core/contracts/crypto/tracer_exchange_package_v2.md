# Tracer Exchange Package v2 (`TTPKG`)

## 状态
1. 状态：Historical / Unsupported
2. 适用范围：仅供历史版本回溯参考。
3. 当前主线：`tracer exchange` 已升级到 `package_version = 3`，请使用：
   - `docs/time_tracer/core/contracts/crypto/tracer_exchange_package_v3.md`

## 说明
1. `v2` package 不再属于当前支持的导入范围。
2. 当前 core / CLI / Android `tracer exchange import` 都按 `v3` 契约执行。
3. 若需要理解当前导出、导入、inspect 行为，请以 `v3` 文档和运行时 JSON 契约为准。
