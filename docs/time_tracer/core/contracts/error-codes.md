# 错误码定义（Core）

This legacy entry has moved to the new shared/capability-first split.

## Read Next
1. `docs/time_tracer/core/shared/error-codes.md`
   - Shared core error-code vocabulary.
2. `docs/time_tracer/core/capabilities/validation/diagnostics.md`
   - Validation-specific diagnostic interpretation and routing.

## Migration Note
New references should prefer `shared/error-codes.md`, with capability-specific
reading routed through `capabilities/<name>/`.
