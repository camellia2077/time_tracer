# Config Capability

This directory is the capability-first entry for `tracer_core` config docs.

Use it when you need to understand:
1. config ownership and owner paths
2. how config participates in validation, query, reporting, and exchange
3. where config loading ends and business-capability use begins

## Read First
1. [overview.md](overview.md)
   - Config ownership, boundaries, and dependency rules.

## Legacy Pointers
Config topics were previously spread across:
1. `core/overview/capability_map.md`
2. `core/overview/module_boundaries.md`
3. `core/capabilities/validation/config_validation.md`
