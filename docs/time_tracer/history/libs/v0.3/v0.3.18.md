## [v0.3.18.6] - [v0.3.18.5]

### 技术改进/重构 (Changed/Refactor)
* **JSON 协议演进**:
    * 优化了 JSON 数据中的键名，弃用 `topparent` 和 `parent`。
    * 与数据库插入逻辑对齐，统一使用 `parent` 和 `child` 命名体系。
    * 剔除了 JSON 中的冗余键以减小体积。


## [v0.3.18.4] - [v0.3.18.2]

### 技术改进/重构 (Changed/Refactor)
* **统计逻辑封装**:
    * 将 `generatedStats` 的统计过程提取为独立方法。
    * 使用 `namespace` 存储需要参与计算的 `parent` 项。
* **配置拆分**: 完成了预处理 JSON 配置的模块化拆分。


## [v0.3.18.1] - [v0.3.18.0]

### 新增功能 (Added)
* **精细化统计**:
    * `generatedStats` 新增洗漱 (`grooming`) 时间统计，属于 `routine` 类别。
    * `generatedStats` 新增上厕所 (`toilet`) 时间统计。