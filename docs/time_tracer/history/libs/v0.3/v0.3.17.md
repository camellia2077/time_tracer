## [v0.3.17.6] - [v0.3.17.5]

### 修复 (Fixed)
* **消除编译警告**:
    * 修复了因 `-Werror` 选项导致的“未使用参数”错误。通过在 **`DayTex**`, **`MonthTex**`, **`PeriodTex**` 的空实现中注释掉参数名称（如 `const MonthlyReportData& /*data*/`）来标记有意未使用。
    * 移除了项目中所有未使用的变量。


## [v0.3.17.4] - [v0.3.17.0]

### 技术改进/重构 (Changed/Refactor)
* **架构模式升级**: 优化程序结构，全面采用 **外观模式 (Facade)** 及其具体实现的分层架构。
* **模块解耦**:
    * 将 **`SourceFileValidator**` 拆分为 `facade` 层与 `pipelines` 层。
    * 优化 **`importer**` (原 `db_inserter`) 的解析器逻辑。
    * 拆分并重组了 `common_utils` 模块。
    * 优化了 `converter` 文件夹的目录结构。