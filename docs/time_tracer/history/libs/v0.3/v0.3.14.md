## [v0.3.14.1] - 2025-09-12

### 新增功能 (Added)
* **运动数据全链路支持**: 更新了数据库插入及报表查询模块，支持 `exercise` 指标的输出。
* **多格式显示**: 在 **Markdown**, **Typst**, **LaTeX** 报表中同步增加了运动数据的展示。


## [v0.3.14.0] - 2025-09-12

### 新增功能 (Added)
* **JSON 协议扩展**: 在 `generatedStats` 节点下新增 `exerciseTime` (秒) 字段，并在 `headers` 中增加 `exercise` 开关。实现初步的运动指标 JSON 转换逻辑。