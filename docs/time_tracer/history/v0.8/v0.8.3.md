## [v0.8.3] - 2026-03-15

### 技术改进/重构 (Changed/Refactor)
* 重构库级架构导航结构，把详细实现说明统一收敛到 `docs/time_tracer/architecture/**`，并把各库本地入口文档收缩为短索引页。
* 重构 `libs/tracer_core/src/application/**`，把 `use_cases` 与 `reporting.tree` 收敛到“module 拥有声明、legacy header 只保留显式兼容边界”的稳定状态。
* 重构 `libs/tracer_core/src/infrastructure/query/data/stats/**`、`libs/tracer_core/src/infrastructure/persistence/**` 与 `libs/tracer_core/src/infrastructure/reports/**`，统一 `modules_on` 主路径到 canonical import，并补齐 thin compatibility boundary 的 smoke coverage。
* 重构 `apps/tracer_core_shell/api/c_api/**`、`apps/tracer_core_shell/host/**` 与 `libs/tracer_core/src/infrastructure/config/internal/**`，把 `config` 相关消费边界收敛为窄 bridge / thin wrapper 形态，降低 `AppConfig` 与 header-world declaration ownership 的外溢。
* 重构 `libs/tracer_core/src/infrastructure/config/internal/**` 与对应 module interface，把 `config_detail_loader`、`config_parser_utils` 收敛为 import-first thin wrapper，并删除剩余 shared declaration fragments。
* 重构 `libs/tracer_core/src/application/workflow*` 双轨实现，把 `modules_on/modules_off` 的镜像实现收敛为共享实现体，并将 `modules_off` 显式固定为 retained fallback 路径。
* 重构 `apps/tracer_core_shell/host/**`、`apps/tracer_core_shell/api/c_api/**` 与 `libs/tracer_core/src/application/ports/i_cli_runtime_factory.hpp`，把宿主侧 `ITracerCoreApi` 消费边界收敛为 import-first / direct-import 主路径，并把剩余 test helper 明确为 retained thin boundary。
* 重构 `apps/tracer_core_shell/tests/platform/infrastructure/tests/android_runtime/**` helper 边界，把 Android runtime smoke helper 从单一 umbrella 聚合收敛为 fixture/query/IO 分层，并明确保留 `android_runtime_test_common.hpp` 作为显式兼容 helper 边界。
* 重构 `libs/tracer_core/src/application/workflow*` 的 fallback 收口判定，明确四个 `modules_off` workflow implementation units 作为最小 retained fallback 保留，不再把继续删除 `.cpp` 视为当前默认主线。
* 重构 `libs/tracer_core/src/infrastructure/modules/**` 物理布局，把 `infrastructure` module surfaces 从单目录平铺收敛为按 `logging`、`platform`、`config`、`persistence`、`query`、`reports` 分组的 family 子目录。
* 重构 `temp/modules_only_toolchain_cutover_readiness*.md`、`temp/tracer_core_module_refactor_master.md` 与 `.agent/guides/docs/tracer_core-handoff.md`，完成 `modules-only toolchain` readiness closeout，并把 `TT_ENABLE_CPP20_MODULES`、`TT_CPP20_MODULES_EFFECTIVE` 与 `TT_FORCE_LEGACY_HEADER_DECLS` 固定为可接受的 retained compat surface。
* 重构 `apps/tracer_core_shell/cmake/ProjectOptions.cmake`、`apps/tracer_core_shell/cmake/ProjectFeatures.cmake`、`apps/tracer_android/runtime/build.gradle.kts` 与 `libs/tracer_core/**` / `libs/tracer_transport/**` / `libs/tracer_adapters_io/**` 的 `modules-only` execution cutover，并以 `temp/archive/module_refactor/modules_only_toolchain_cutover_execution.md` closeout 固化 `TT_ENABLE_CPP20_MODULES`、`TT_CPP20_MODULES_EFFECTIVE` 与 `TT_FORCE_LEGACY_HEADER_DECLS` 的 retained compat 结论。
* 重构 `libs/tracer_core/src/application/**`、`libs/tracer_core/src/infrastructure/**`、`libs/tracer_adapters_io/src/infrastructure/io/**` 与 `docs/time_tracer/clients/windows_cli/specs/console-color.md`，将 raw ANSI 颜色常量从 `tracer_core` stable contract 降级为表现层职责，并统一收口到 `severity + plain UTF-8 message` 主路径。
* 重构 `libs/tracer_core/src/shared/utils/string_utils.hpp`、`libs/tracer_core/src/shared/modules/tracer.core.shared.string_utils.cppm` 与 `libs/tracer_core/src/domain/logic/**` 的剩余 consumer，把 `Trim` / `SplitString` 的 owner 收回 `tracer::core::shared::string_utils`，并将遗留 header include 消费收敛到 direct module import。
* 完成 `temp/archive/module_refactor/tracer_core_pure_module_only_reassessment.md` closeout，确认 `libs/tracer_core` 已达到 pure internal module-only，剩余 thin headers / compat tests 仅再作为显式 outer-boundary surface 保留，并把后续去 compat 判断上升为 `Goal 2` 生态级议题。
* 完成 `Goal 2 / Phase 2 / front-door removal`：移除 `apps/tracer_core_shell/cmake/ProjectOptions.cmake` / `ProjectFeatures.cmake` 的 modules front door，删除 Android `timeTracerEnableCpp20Modules` 兼容属性入口，并同步压平 `libs/tracer_transport/**` 与 `libs/tracer_adapters_io/**` 对 `TT_CPP20_MODULES_EFFECTIVE` 的 build gate 依赖。
* 重构 `apps/tracer_core_shell/api/c_api/**`、`apps/tracer_core_shell/host/**` 与 `apps/tracer_core_shell/api/android_jni/**`，完成 `Goal 2 / Phase 3` 的 shell/runtime consumer cutover，把 `ITracerCoreApi` 消费、runtime config snapshot、JNI code-to-wire 翻译与 crypto progress payload 投影收敛到 direct module import 或 app-owned bridge boundary。
* 重构 `libs/tracer_core_bridge_common/**` 与相关 CMake 装配，明确 `bridge_common` 只保留 `tracer_transport` canonical helper 责任，不再承接 C API parse helper、JNI code-value translation 或 crypto progress JSON projection。
* 重构 `libs/tracer_adapters_io/**` 与 `apps/tracer_core_shell/host/android_runtime_factory.cpp`，新增 `tracer.adapters.io.runtime` canonical module surface，并将 runtime adapter concrete declarations 内收到 `src/infrastructure/io/internal/runtime_adapter_types.hpp`，让 host runtime assembly 脱离 legacy header consumer path。
* 重构 `apps/tracer_core_shell/cmake/CoreTargets.cmake` 与 `libs/tracer_core/tests/**`，用 module smoke + stable contract regression 取代 `headers_smoke` / `legacy_headers_compat` compat-only test matrix。
* 更新 `docs/time_tracer/architecture/**`、`docs/time_tracer/guides/native/native_modules.md`、`docs/time_tracer/core/specs/AGENT_ONBOARDING.md` 与 `.agent/guides/docs/tracer_core-*.md`，把 `Goal 2` closeout 后的默认状态统一为 module-first + explicit stable boundary。
* 重构 `libs/tracer_core/src/infrastructure/query/**`、`libs/tracer_core/src/infrastructure/config/**`、`libs/tracer_core/src/infrastructure/reports/**`、`libs/tracer_core/src/application/use_cases/**`、`libs/tracer_core/src/application/workflow_handler*`、`libs/tracer_core/src/application/reporting/tree/**`、`libs/tracer_core/tests/**` 与 `apps/tracer_core_shell/tests/platform/infrastructure/tests/**` 的 residual consumer，完成 `module-first / import-expansion` Wave 1-10 收口，并把低风险 non-boundary consumer 主路径进一步统一到 `import tracer.*`。
* 归档 `temp/tracer_core_module_first_*` 主题文档到 `temp/archive/module_refactor/tracer_core_module_first_import_expansion_2026-03-17/`，并新增 `temp/tracer_core_module_first_guardrails_canonical_surface_plan.md`，将后续默认主线从机械 consumer sweep 切换为 `module-first guardrails / canonical surface ergonomics`。
* 重构 `temp/tracer_core_module_first_guardrails_*` 主题线、`libs/tracer_core/src/application/service/log_processor.cpp`、`libs/tracer_core/src/domain/logic/validator/txt/facade/text_validator.cpp` 与 `docs/time_tracer/architecture/**` / `docs/time_tracer/core/specs/AGENT_ONBOARDING.md`，完成 `module-first guardrails / canonical surface ergonomics` 的 inventory、ergonomics audit、consumer-side pilot、review-summary hook 与入口文档 integration，明确后续不默认进入 broad wrapper redesign 或自动 guardrail enforcement。
* 归档 `temp/tracer_core_module_first_guardrails_*` 完成产物到 `temp/archive/module_refactor/tracer_core_module_first_guardrails_2026-03-17/`，并新增 `temp/tracer_core_boundary_owned_residual_canonical_surface_reassessment.md`，将下一条默认主线切换为 `boundary-owned residual canonical surface reassessment`。

### 修复 (Fixed)
* 修复 `workflow`、`query.data.stats`、`persistence.*`、`reports.*`、`use_cases` 与 `reporting.tree` 仍由 shared declaration fragments、legacy alias API 或 legacy headers 承接内部主路径的问题，统一到 import-first 的兼容边界契约。
* 修复 Android host 与 C API 继续直接依赖完整 `AppConfig` 或在同一 TU 混用 config bridge 与 `nlohmann/json` 导致的 `config` imported-path / ownership 扩散问题。
* 修复 `modules_off` 构建仍误编 logging module implementation，以及少量 persistence / shell test TU 继续无条件 import module 的问题，恢复 focused validate 双轨通过。
* 修复 Android runtime bridge 未编入 runtime config helper 源文件导致的链接失败问题，恢复 `tracer_android` 构建通过。
* 修复 infrastructure module smoke test 的运行时配置路径与 `ConfigLoader` 约定不一致的问题，恢复 `tc_infra_mod_smoke_tests.exe` 直接运行通过。
* 修复 `apps/tracer_core_shell/api/c_api/tracer_core_c_api_runtime_config.cpp`、`apps/tracer_core_shell/host/android_runtime_factory_catalog.cpp`、`android_runtime_factory_resolver.cpp`、`apps/tracer_core_shell/api/android_jni/native_bridge.cpp` 与 `apps/tracer_core_shell/host/native_bridge_progress.cpp` 继续直接依赖 core config internals 或 shared bridge helper 的边界泄漏问题，恢复 C ABI / Android runtime 路径只经显式 bridge boundary 暴露稳定 contract。
* 修复 `libs/tracer_adapters_io` runtime assembly 仍通过 retained legacy headers 与 dead compat declarations 暴露 concrete adapter types 的问题，统一主路径到 `tracer.adapters.io.runtime` factory 与 module-facing round-trip smoke regression。

### 弃用/删除 (Deprecated/Removed)
* 删除 `libs/tracer_core/src/shared/detail/ansi_colors_contract.inc`、`libs/tracer_core/src/shared/types/ansi_colors.hpp` 与 `libs/tracer_core/src/shared/modules/tracer.core.shared.ansi_colors.cppm`，不再把 ANSI 常量作为 `tracer_core` shared canonical contract 对外暴露。
* 删除 `libs/tracer_core/src/shared/detail/string_utils_contract.inc`，不再承认 global `Trim` / `SplitString` 作为 `tracer_core` shared stable compat contract。
* 删除 `libs/tracer_core_bridge_common/src/c_api/parse_utils.*` 与 `libs/tracer_core_bridge_common/src/shared/crypto_progress_json.hpp`，不再把 C API parse helper 与 crypto progress projection 当作 shared public helper surface。
* 删除 `libs/tracer_adapters_io/src/infrastructure/io/file_io_service.hpp`、`file_io_service.cpp`、`processed_data_io.hpp`、`txt_ingest_input_provider.hpp` 与 `libs/tracer_adapters_io/tests/adapters_io_legacy_headers_compat_tests.cpp`，不再承认 adapter legacy headers 与 compat-only tests 作为推荐 consumer path。
* 删除 `libs/tracer_core/tests/**` 下全部 `*headers_smoke_tests.cpp`、`*legacy_headers_compat_tests.cpp` 与 `tests/infrastructure/tests/legacy_headers_compat/**` support wiring，不再保留 `headers_smoke` / `legacy_headers_compat` 迁移矩阵。
* 删除 `docs/time_tracer/architecture/**`、`docs/time_tracer/guides/native/native_modules.md`、`docs/time_tracer/core/specs/AGENT_ONBOARDING.md` 与 `.agent/guides/docs/tracer_core-*.md` 中仍把 `TT_ENABLE_CPP20_MODULES`、`TT_CPP20_MODULES_EFFECTIVE`、`TT_FORCE_LEGACY_HEADER_DECLS`、legacy header 或 thin wrapper 写成当前支持面的口径。
