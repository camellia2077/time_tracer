## 2026-04-04

### 技术改进/重构 (Changed/Refactor)
* 重构 `tools/toolchain/commands/tidy/autofix/`，按“规则计划（rules）-意图模型（models）-执行引擎（engines）-编排（orchestrator）”拆分职责，统一通过 `registry` 装配并由 `run_task_auto_fix_orchestrator` 执行。
* 新增 `tools/toolchain/commands/tidy/autofix/rules/catalog.py` 与 `tools/toolchain/commands/tidy/autofix/reasons.py`，把规则元数据、风险级别与执行原因集中管理，减少规则实现内的重复常量与分支。
* 新增 `tools/toolchain/commands/tidy/autofix/rules/concise_preprocessor_directives.py`，把 `readability-use-concise-preprocessor-directives` 纳入统一规则编排链路。
* 重构 `tools/toolchain/commands/tidy/task_auto_fix.py`、`task_auto_fix_plan.py`、`task_auto_fix_apply.py`、`task_auto_fix_report.py`、`task_auto_fix_types.py`，将 plan/apply/report 与 orchestrator 解耦并保持兼容导出。
* 拆分 `tools/toolchain/commands/tidy/task_record_parse.py` 为 `tools/toolchain/commands/tidy/parse_common.py`、`parse_json.py`、`parse_text.py`，将任务记录解析按输入格式分层。
* 重构 `tools/toolchain/commands/tidy/step.py`，把 step 内部流程下沉到 `tools/toolchain/commands/tidy/step_internal/`，降低单文件复杂度并明确阶段边界。
* 重构 `tools/toolchain/commands/analyze/command.py`，拆分为 `tools/toolchain/commands/analyze/orchestrator.py`、`report_service.py`、`unit_runner.py`，统一命令编排与报告职责。
* 重构平台与 CLI 测试组织：将聚合测试拆分为按子域文件（如 `tools/tests/run_cli/test_run_cli_dispatch_*.py`、`tools/tests/platform/test_tidy_task_*.py`），并新增通用 fixture builder（`tools/tests/platform/_tidy_task_auto_fix_support.py`）统一 `plan/apply/orchestrator` 测试样板。

### 修复 (Fixed)
* 修复 `tools/toolchain/commands/tidy/autofix/rules/using_namespace.py` 的符号收集与替换策略，使 `dto` 与 `std::chrono` 场景都能生成稳定的 `using` 声明。
* 修复 `tools/toolchain/commands/tidy/autofix/engines/text_edit_engine.py` 与 `clangd_rename_engine.py` 的执行记录回填语义，统一 `previewed/applied/failed` 状态与变更明细输出。
* 修复 `tools/devtools/loc_scanner/src/loc_scanner/cli_app.py` 的路径解析流程，并补齐 `tools/devtools/loc_scanner/tests/test_path_resolution.py` 回归覆盖。

### 弃用/删除 (Deprecated/Removed)
* 删除聚合式旧测试文件 `tools/tests/run_cli/test_run_cli_dispatch.py`，改为按命令族拆分的 `test_run_cli_dispatch_*` 测试集。
* 删除聚合式旧测试文件 `tools/tests/platform/test_tidy_task_automation.py` 与 `tools/tests/platform/test_tidy_task_model.py`，改为按职责拆分后的 `test_tidy_task_*` 测试集。
* 删除旧测试文件 `tools/tests/platform/test_build_toolchain_flags.py`，改为分拆后的 `test_build_toolchain_flags_resolve.py` 与 `test_build_cmake_*` 相关用例。
