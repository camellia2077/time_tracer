## 2026-09-12

### 技术改进/重构 (Changed/Refactor)
* 重构 GitHub Actions CI 流水线架构，删除旧 `.github/workflows/windows-build-matrix.yml`，拆分为 `.github/workflows/android-ci.yml`、`.github/workflows/libs-ci.yml` 和 `.github/workflows/cli-ci.yml` 三个独立工作流，实现各端改动的按需触发与并行验证。
* 迁移 `libs` 验证环境，将 `libs-core-verify` 作业执行环境由 Windows 调整为 `ubuntu-latest`，采用 `python tools/run.py verify --app tracer_core_shell --profile libs_ci_no_pch --build-dir build_libs --scope libs --concise` 完成 Linux 环境下的核心库与 bridge 测试验证。
* 独立 Windows CLI 验证流水线至 `.github/workflows/cli-ci.yml`，通过 `.github/actions/run-windows-verify` 和 `.github/actions/setup-windows-verify-env` 承接 Windows 平台的完整验证与产物对账。

### 弃用/删除 (Deprecated/Removed)
* 删除旧 `.github/workflows/windows-build-matrix.yml` 单一多任务矩阵流水线。
