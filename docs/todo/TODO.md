00
优先解决安卓month/week这些报告时间和windows不一致的问题




0
安卓表现层，在输入查询相关的时候
如果确定了月份？，应该提示最多有几天，不然用户记不住
每一个月最多几天，以及2月这种特殊情况

1
目前安卓那边为了方便用户编辑txt，在底部的TXT tabs实现了一个简单的txt editor
这个应不应该并入内核？但是windows那边又不需要程序自带txt编辑，因为windows的记事本或者vscode体验都很好，不需要重复造轮子。
我希望安卓那边只是个表现和io层，不要承担业务逻辑，因为cpp写业务逻辑速度肯定比kt/java速度快，但是安卓txt好像也不需要大量的计算资源，好像不并入tracer_core也行？
应该怎么办，我们讨论一下。

3
目前存储txt是这样的
├── 2025
│   ├── 2025-01.txt
│   ├── 2025-02.txt
省略2025-03.txt到2025-12.txt
txt存储是是这个月中每一天的具体时间和对应的活动内容
一个txt几百行，因为windows那边屏幕大，android屏幕小
所以我在安卓表现层增加了一个功能，就是通过输入这个月的的具体日期
比如0201就只显示0201这一天记录的内容。

现在问题是，程序之前检验的时候，给出错误信息都是按照那个完整的txt来给出具体行数的，这虽然方便了windows_cli中直接按照具体行数在ide中跳转

但是这个对于安卓那边不友好，比如我正在处理0201这一小部分的内容
如果报错，我还要切换到2月txt的完整内容来去查看
而且现在安卓那边的txt editor是没有行数的

目标是不是让安卓那边先支持txt editor中的行数显示，以及插入内容，检验的时候
是根据当前是选择全部txt还是部分txt来输出不同的行数？






9之前活动的备注的字符是硬编码的，现在应该从toml中加入配置，更新
tracer_core



8注释的内容用toml来规定
比如 # // ;
这需要修改内核


10
core现在应该支持一个功能，就是展现tree的时，不光存储每个root的子节点，也存储对应root对应的存储时间，因为目前只支持展开没有时间的tree结构
是否应该先在windows_cli表现层实现是否展示每个root对应存储的时间
然后在去给安卓表现层加入对应的功能？

还有就是格式化报告的时候，也会输出类似tree结构的内容
比如
### study: 3h 48m (22.0%) ###
- math: 2h 30m
  - linear-algebra: 2h 30m
    - matrix: 2h 30m
- english: 1h 18m
  - article: 1h 18m

所以说内核处理格式化报告活动tree结构和支持tree相关命令的，是不是应该复用代码？目前程序做到这一点了吗




tree的结构在windows_cli输出状态如下
exercise
├── anaerobic
└── cardio
meal
└── dining
recreation
├── game
│   ├── clash-royale
│   ├── minecraft
│   └── overwatch
└── online-platforms
    ├── bilibili
    ├── douyin
    └── zhihu

我们来讨论一下当前的问题




11
感觉意义不是很大

以后时间准确度可以精确到秒
时间记录由例如
2301 活动名
变为
230134 活动名


不要立刻默认切秒，先保持分钟为默认。
如果要做秒，必须“内核先改，再 Android 开关落地”。
兼容策略用你说的方式: 4 位自动补 00 秒（2301 -> 23 (line 1, column 0)），6 位按真实秒。
展示层建议默认仍显示 HH:mm，减少报告噪声；需要时再开“显示秒”。
这样你能兼容旧数据，又不会把 CLI/Android/报告一致性打乱。









增加一个插入并且导出全部日期全部格式的命令


高优先级



3
自定义标题的输出顺序
reports文件夹中json自定义格式化输出的标题，输出为true ,不输出为false





-1
import的时候要检查是否为所需的 json文件，以免用户import了txt文件

7
使用字体的时候注意，可变字体必须是当前字体支持的语言
你用新罗马那肯定不能显示中文字体
用宋体也是不支持西文字体可变
用新罗马才支持西文字体可变

tex无法编译是因为不支持当前的可变字体，tex内容由加粗
解决方法就是代码中取消注释，手动指定字体的目录，但是这样只能编译，无法正确渲染字体加粗


2导出的日报告的时候，没用按照年份文件夹，月份文件夹来分类


低优先级


## 让tex支持生成的字体从本地获取
\setCJKmainfont[Path=C:/]{NotoSerifSC-VariableFont_wght.ttf}

## 格式化输出，根据类型输出不同的颜色背景。
没有任何一个实现
