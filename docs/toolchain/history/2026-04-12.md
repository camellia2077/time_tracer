## 2026-04-12

### 修复 (Fixed)
* 修复 `python tools/run.py verify --app tracer_core` 在改动 `tools/suites/**`、`tools/test_framework/**`、`tools/tests/**` 等测试基建路径时被错误收敛到 `cap_pipeline` 的回归；这类改动现在会回退到完整 `fast` 验证，重新执行 `artifact_windows_cli` 的 host black-box 与 golden / parity / artifact compare 流程。
* 修复 `out/test/artifact_windows_cli/` 只产出 `logs/1_pipeline` 和少量结果的回归；`fast` 验证现在会重新跑出 `report`、`report-export`、`txt-view-day`、`version`、`tree`、`failure`、`query-data`、`exchange` 等 CLI 盲盒阶段，并恢复 `quality_gates/` 下的 markdown / triplet 对账产物。
* 修复迁移到 `tools/` 后 `tools/tests/run_cli/test_contract_guardrails.py` 与 `tools/tests/platform/core/test_test_suite_command_order.py` 仍引用旧 `test/suites/tracer_windows_rust_cli` 和旧 `framework.*` 导入的残留问题，使工具链回归测试重新对齐 `tools/suites/tracer_windows_rust_cli/` 与 `tools.test_framework.*`。
