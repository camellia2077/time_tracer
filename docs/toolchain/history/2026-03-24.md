## 2026-03-24

### 技术改进/重构 (Changed/Refactor)
* 重构 `tools/toolchain/commands/cmd_validate/command.py`，拆分 `step_runner.py`、`summary.py` 与 `verify_track.py`，把轨道执行、日志捕获、结果汇总与 artifact 收集从单文件中拆开。
* 重构 `tools/toolchain/commands/tidy/task_auto_fix.py` 与 `tools/toolchain/commands/tidy/task_model.py`，拆分 `task_auto_fix_plan.py`、`task_auto_fix_apply.py`、`task_auto_fix_report.py`、`task_record_parse.py`、`task_record_render.py`、`task_record_types.py` 等模块，收敛 auto-fix 与 task record 的职责边界。
* 收敛 `verify` 工作流，移除 `tools/toolchain/cli/handlers/quality/verify.py` 的 `--scope` 入口，并把 `tools/toolchain/commands/cmd_quality/verify.py` 固定为 `build -> unit_checks -> artifact checks` 的顺序执行模型。
* 调整 `tools/toolchain/commands/tidy/step.py` 的 apply 流程，把 task-scope verify 替换为 build sanity check，再执行 post-build clang-tidy re-check 与任务归档。
* 为 `self-test` 增加 `--group` 命名测试组，并新增 `tools/tests/groups.py` 统一维护 `verify-stack`、`platform`、`validate`、`verify`、`run-cli` 分组。
* 更新 `tools/README.md`、`tools/scripts/README.md` 与 `tools/toolchain/README.md`，同步 `verify` 与 `self-test` 的最新命令入口和执行语义。

### 修复 (Fixed)
* 补齐 `tools/tests/verify/test_self_test_command.py`，并更新 `tools/tests/verify/test_verify_execute_flow.py`、`tools/tests/run_cli/test_run_cli_dispatch.py`、`tools/tests/platform/test_tidy_step.py`、`tools/tests/validate/test_validate_command.py` 与 `tools/tests/validate/test_validate_plan.py`，使回归测试与新的 verify、validate、tidy-step 行为保持一致。
