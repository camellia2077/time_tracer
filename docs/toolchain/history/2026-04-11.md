## 2026-04-11

### 技术改进/重构 (Changed/Refactor)
* 迁移 suite 入口与测试工具链实现，把 `test/run.py` 重构为 `tools/test.py`，把 `test/framework/**` 重构为 `tools/test_framework/**`，并把 `test/suites/**` 迁移到 `tools/suites/**`。
* 重构 `tools/toolchain` 的 suite 调用与 owner/profile 推断逻辑，使 `verify`、`validate`、`tidy loop`、suite loader 与 command/profile 映射统一使用 `tools/suites/**` 新路径。
* 重写 `tools/README.md`、`tools/AGENTS.md`、`test/README.md`、`test/AGENTS.md` 与 `docs/toolchain/test/*.md` 的目录边界说明，把 `test/` 收口为共享测试资产层，把 suite 资产与 runner 实现收口到 `tools/`。

### 修复 (Fixed)
* 修复 reporting quality gates 仍引用 `test/suites/tracer_windows_rust_cli/tests/gate_cases.toml` 的旧路径问题，更新 `report_markdown_gate`、`verify_markdown_gate_runner`、`refresh_golden` 与 reporting case collector，使 `validate`/golden gate 在 `tools/suites/**` 迁移后继续可执行。

### 弃用/删除 (Deprecated/Removed)
* 删除 `test/run.py`、`test/framework/**`、`test/suites/**` 与 `test/suites/lint_suites.py` 旧布局，不保留向前兼容入口；suite 执行统一改为 `python tools/test.py ...`，suite schema lint 统一改为 `python tools/lint_suites.py ...`。
