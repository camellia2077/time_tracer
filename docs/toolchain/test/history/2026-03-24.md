## 2026-03-24

### 技术改进/重构 (Changed/Refactor)
* 扁平化 `test/framework/core` 包结构，移除旧 `test/framework/core/core/` 中间层，把 `base.py`、`command_expander.py`、`command_expectations.py`、`command_validation.py` 与 `json_payload.py` 上移到包根目录。
* 重构 `test/framework/core/runtime/` 与 `test/framework/core/suite/` 的装配边界，使执行器、日志路由、报告器与 suite engine 通过新的包根导入路径协同工作。
* 更新 `test/framework/core/main.py` 与 `test/framework/core/cases/export.py`、`ingest.py`、`query.py`、`version.py` 的导入路径，使入口、case 与 suite 统一指向新的 `test/framework/core` 目录布局。

### 弃用/删除 (Deprecated/Removed)
* 删除旧 `test/framework/core/core/` 兼容层及其下的 `base.py`、`runtime/*`、`suite/*` 等重复路径，避免测试框架同时维护两套模块入口。
