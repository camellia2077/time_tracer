from __future__ import annotations

REPORT_PERIOD_KEYS: tuple[str, ...] = ("day", "month", "period", "week", "year")
REPORT_FORMAT_ORDER: tuple[str, ...] = ("markdown", "latex", "typst")
SUPPORTED_TARGETS: tuple[str, ...] = ("windows", "android")
