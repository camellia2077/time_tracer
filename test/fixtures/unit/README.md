# test/fixtures/unit

Unit/component test fixtures only.

Rules:
- Keep fixtures minimal and single-purpose.
- Do not copy the large integration dataset from `test/data`.
- Prefer inline literals in tests when file fixtures are not necessary.
