[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # 基础风格
    "F",   # 语法错误
    "B",   # 逻辑漏洞 (Bugprone)
    "UP",  # 现代化 (Modernize)
    "I",   # 排序 Import
    "A",   # 防止覆盖内置变量
    "N",   # 命名规范
]
ignore = [
    "E501",  # 交给 ruff format 处理长行
]
