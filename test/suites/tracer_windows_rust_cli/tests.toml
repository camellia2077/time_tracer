# Aggregated scenario configuration for tracer_windows_rust_cli suite.
# Include order matters for merged command execution order.
includes = [
  "tests/base.toml",
  "tests/commands_reporting.toml",
  "tests/commands_version.toml",
  "tests/commands_query_tree.toml",
  "tests/commands_failure_cli.toml",
  "tests/commands_failure_runtime.toml",
  "tests/commands_query_data.toml",
  "tests/commands_exchange.toml",
]
