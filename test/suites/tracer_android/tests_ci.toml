# Extra checks for android_ci profile

[[commands]]
name = "CI Report LintRelease Exists"
stage = "android-ci"
args = [
  "/c",
  "if exist ..\\..\\..\\..\\apps\\tracer_android\\app\\build\\reports\\lint-results-release.html (exit /b 0) else (echo Missing lint-results-release.html && exit /b 1)"
]
expect_exit = 0
