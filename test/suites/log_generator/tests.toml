# Aggregated scenario configuration for log_generator suite.
includes = [
  "tests/base.toml",
  "tests/commands_version.toml",
  "tests/command_groups_generate.toml",
  "tests/commands_average_guard.toml",
]
