package com.example.tracer

/** The independent period selected for an Activities view or comparison. */
data class InsightsPeriodSelection(
    val date: String,
    val month: String,
    val year: String,
    val week: String
)
