parent = "sleep"

[canonical]
"night" = ["wake", "w", "起床", "醒", "新的一天开始了"]

[sleep_inference]
# [canonical] 中的所有 alias 都是起床关键词，Core 会自动生成内部 wake 集合。
# 普通睡眠活动应配置在 activity_hierarchy/sleep.toml 中。
sleep_project_path = "sleep_night"
