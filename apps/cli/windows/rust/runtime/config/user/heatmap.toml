schema_version = 1

[thresholds]
# Bucket mapping rule:
# - colors[0] : value <= 0
# - colors[1] : 0 < value <= positive_hours[0]
# - colors[2] : positive_hours[0] < value <= positive_hours[1]
# - ...
# - colors[last] : value > positive_hours[last]
positive_hours = [1.0, 4.0, 7.0, 9.0]
