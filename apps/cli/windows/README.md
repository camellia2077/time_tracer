# tracer_windows_rust_cli

The active Windows CLI is the Rust client in `apps/cli/windows/rust/**`, hosted
by `tracer_core.dll`.

Use these authoritative sources instead of maintaining a second command or
workflow description here:

- Agent routing, invariants, validation, evidence, and completion:
  `apps/cli/windows/AGENTS.md`
- Current command surface and user-visible behavior:
  `docs/time_tracer/clients/windows_cli/README.md`
- Structure and ownership:
  `docs/time_tracer/clients/windows_cli/specs/STRUCTURE.md`
- Runtime ABI:
  `docs/time_tracer/core/shared/c_abi.md`
