name: Windows Build Matrix

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * 0"

jobs:
  detect-changes:
    name: detect-changes
    runs-on: ubuntu-latest
    outputs:
      # These outputs drive the whole Windows verify fan-out:
      # docs/ui-only changes can skip heavy Windows work, focused owner paths can
      # run a small capability smoke matrix, and everything else can fall back to
      # the full heavy verify matrix.
      run_heavy_verify: ${{ steps.capability.outputs.run_heavy_verify }}
      run_capability_smoke: ${{ steps.capability.outputs.run_capability_smoke }}
      capability_mode: ${{ steps.capability.outputs.capability_mode }}
      capability_matrix: ${{ steps.capability.outputs.capability_matrix }}
      capability_reason: ${{ steps.capability.outputs.capability_reason }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup shared Python repo tools
        uses: ./.github/actions/setup-python-repo-tools

      - name: Compute capability smoke profiles
        id: capability
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          PAYLOAD="$(python -m tools.toolchain.ci.capability_smoke_profiles --repo-root . --event-name "$EVENT_NAME" --base-sha "$BASE_SHA" --head-sha "$HEAD_SHA")"
          export PAYLOAD
          echo "payload=${PAYLOAD}"
          python - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import os

          payload = json.loads(os.environ["PAYLOAD"])
          print(f"run_heavy_verify={'true' if payload['run_heavy_verify'] else 'false'}")
          print(f"run_capability_smoke={'true' if payload['run_capability_smoke'] else 'false'}")
          print(f"capability_mode={payload['mode']}")
          print(f"capability_matrix={payload['matrix']}")
          print(f"capability_reason={payload['reason']}")
          PY

      - name: Show capability smoke decision
        shell: bash
        run: |
          echo "capability_mode=${{ steps.capability.outputs.capability_mode }}"
          echo "run_capability_smoke=${{ steps.capability.outputs.run_capability_smoke }}"
          echo "capability_reason=${{ steps.capability.outputs.capability_reason }}"
          echo "capability_matrix=${{ steps.capability.outputs.capability_matrix }}"

  prepare-windows-matrix:
    name: prepare-windows-matrix
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.generate.outputs.matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup shared Python repo tools
        uses: ./.github/actions/setup-python-repo-tools

      - name: Generate matrix from toolchain config
        id: generate
        shell: bash
        run: |
          MATRIX_JSON="$(python -m tools.toolchain.ci.windows_matrix --config tools/toolchain/config/build.toml)"
          echo "matrix=${MATRIX_JSON}" >> "$GITHUB_OUTPUT"

  windows-capability-smoke:
    needs:
      - detect-changes
    if: needs.detect-changes.outputs.run_capability_smoke == 'true'
    # This is an early, owner-focused gate for high-reuse capabilities. It is
    # intentionally narrower than the full Windows matrix and exists to surface
    # core/runtime regressions before the expensive matrix fan-out starts.
    name: capability-smoke
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.detect-changes.outputs.capability_matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup shared Windows verify environment
        uses: ./.github/actions/setup-windows-verify-env

      - name: Focused capability verify
        shell: pwsh
        run: |
          python tools/run.py verify --app tracer_core_shell --profile ${{ matrix.profile }} --build-dir build_fast --concise

  windows-matrix:
    needs:
      - detect-changes
      - prepare-windows-matrix
      - windows-capability-smoke
    name: ${{ matrix.job_name }}
    runs-on: windows-latest
    # Keep the heavy matrix visible for both real verify runs and docs-only skips.
    # The capability smoke dependency is allowed to be success/skipped/empty so
    # that non-focused changes do not get blocked by a smoke job that never ran.
    if: ${{ always() && (needs.detect-changes.outputs.run_heavy_verify == 'true' || needs.detect-changes.outputs.run_heavy_verify == 'false') && (needs.windows-capability-smoke.result == 'success' || needs.windows-capability-smoke.result == 'skipped' || needs.windows-capability-smoke.result == '') }}
    continue-on-error: ${{ matrix.allow_failure }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.prepare-windows-matrix.outputs.matrix) }}

    steps:
      - name: Skip heavy verify for docs/ui-only change
        if: needs.detect-changes.outputs.run_heavy_verify != 'true'
        shell: pwsh
        run: |
          Write-Host "Skipping Windows verify because this push/PR only changed docs/ui or other non-code paths."

      - name: Checkout
        if: needs.detect-changes.outputs.run_heavy_verify == 'true'
        uses: actions/checkout@v5

      - name: Setup shared Windows verify environment
        if: needs.detect-changes.outputs.run_heavy_verify == 'true'
        uses: ./.github/actions/setup-windows-verify-env

      # Empty cmake_args means the standard baseline row. Non-empty cmake_args are
      # reserved for experimental/variant rows so the default path stays explicit.
      - name: Verify tracer_core_shell (stable rows)
        if: needs.detect-changes.outputs.run_heavy_verify == 'true' && matrix.cmake_args == ''
        shell: pwsh
        run: |
          python tools/run.py verify --app tracer_core_shell --profile ${{ matrix.profile }} --build-dir ${{ matrix.build_dir }} --concise

      - name: Verify tracer_core_shell (experimental row)
        if: needs.detect-changes.outputs.run_heavy_verify == 'true' && matrix.cmake_args != ''
        shell: pwsh
        run: |
          python tools/run.py verify --app tracer_core_shell --profile ${{ matrix.profile }} --build-dir ${{ matrix.build_dir }} --concise ${{ matrix.cmake_args }}

      - name: Show markdown audit report
        if: always() && needs.detect-changes.outputs.run_heavy_verify == 'true'
        shell: pwsh
        run: |
          $p = "temp/report-md-golden-byte-audit.md"
          if (Test-Path $p) {
            Write-Host "===== $p ====="
            Get-Content $p
          } else {
            Write-Host "Audit report not found: $p"
          }

      - name: Upload markdown audit report
        if: always() && needs.detect-changes.outputs.run_heavy_verify == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: report-md-golden-byte-audit-${{ matrix.job_name }}
          path: temp/report-md-golden-byte-audit.md
          if-no-files-found: warn
