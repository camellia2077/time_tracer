name: Setup Python Repo Tools
description: Prepare Python and install the repository toolchain package.

runs:
  using: composite
  steps:
    - name: Setup Python
      uses: actions/setup-python@v6
      with:
        python-version: "3.11"

    - name: Install repo tools package
      shell: bash
      run: |
        python -m pip install -e .
