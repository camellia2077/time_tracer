# tracer_core_bridge_common tests

Phase 4 Step 4.2 baseline:

- Current migration moved library-level tests from `apps/tracer_core/src/**/tests`
  into `libs/tracer_core/tests/**`.
- No existing `apps` test cases were classified as pure
  `tracer_core_bridge_common` unit tests yet.
- Add future bridge-common tests under this directory.
